export interface Message {
  message: string;
}

export interface User {
  // uid: string;
  email: string;
  // displayName: string;
  givenName: string;
  familyName: string;
  emailVerified: boolean;
}
